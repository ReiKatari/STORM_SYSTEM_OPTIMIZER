<Page x:Class="StormSystemOptimizer.Views.BrowserExtensionsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:vm="clr-namespace:StormSystemOptimizer.ViewModels"
      Title="Менеджер расширений браузеров">

    <Page.DataContext>
        <vm:BrowserExtensionsViewModel/>
    </Page.DataContext>

    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="20">
        <StackPanel>
            <!-- Header Card -->
            <Border Background="{DynamicResource CardBackgroundBrush}" BorderBrush="{DynamicResource CardBorderBrush}" BorderThickness="1" CornerRadius="12" Padding="20" Margin="0,0,0,16">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Margin="0,0,24,0" VerticalAlignment="Center">
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                            <Border Background="#1A38BDF8" BorderBrush="#3338BDF8" BorderThickness="1" CornerRadius="6" Width="28" Height="28" Margin="0,0,8,0" VerticalAlignment="Center">
                                <Path Data="{StaticResource GeoBrowser}" Fill="{StaticResource IconGradSky}" Width="14" Height="14" Stretch="Uniform" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <TextBlock Text="Менеджер расширений и дополнений браузеров" FontSize="20" FontWeight="Bold" Foreground="{DynamicResource TextPrimaryBrush}" VerticalAlignment="Center"/>
                        </StackPanel>
                        <TextBlock Text="Централизованный аудит всех расширений во всех профилях Chrome, Edge, Яндекс Браузера, Firefox, Brave и Opera. Определение шпионских и рекламных модулей с удалением в 1 клик."
                                   FontSize="13" Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,0,0,10" TextWrapping="Wrap"/>
                        <TextBlock Text="{Binding StatusMessage}" FontSize="12" FontWeight="SemiBold" Foreground="{DynamicResource AccentPrimaryBrush}" TextWrapping="Wrap"/>
                    </StackPanel>

                    <WrapPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                        <Button Command="{Binding ScanExtensionsCommand}" Style="{StaticResource StormSecondaryButton}" Padding="14,10" Margin="0,0,8,4">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <Path Data="{StaticResource GeoScanner}" Fill="{StaticResource IconGradCyan}" Width="11" Height="11" Stretch="Uniform" Margin="0,0,6,0"/>
                                <TextBlock Text="Сканировать" FontSize="11.5" FontWeight="Bold" Foreground="{DynamicResource TextPrimaryBrush}"/>
                            </StackPanel>
                        </Button>
                        <Button Command="{Binding RemoveAllSuspiciousCommand}" Style="{StaticResource StormHeroButton}" Padding="16,10" Margin="0,0,0,4">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <Path Data="{StaticResource GeoGarbage}" Fill="#FFFFFF" Width="11" Height="11" Stretch="Uniform" Margin="0,0,6,0"/>
                                <TextBlock Text="Очистить подозрительные" FontSize="11.5" FontWeight="Bold" Foreground="#FFFFFF"/>
                            </StackPanel>
                        </Button>
                    </WrapPanel>
                </Grid>
            </Border>

            <!-- Metrics Summary Grid -->
            <UniformGrid Columns="3" Margin="0,0,0,16">
                <Border Background="{DynamicResource CardBackgroundBrush}" BorderBrush="{DynamicResource CardBorderBrush}" BorderThickness="1" CornerRadius="10" Padding="14,10" Margin="0,0,6,0">
                    <StackPanel>
                        <TextBlock Text="Всего расширений в системе" FontSize="11" FontWeight="SemiBold" Foreground="{DynamicResource TextSecondaryBrush}"/>
                        <TextBlock Text="{Binding TotalExtensionsCount}" FontSize="18" FontWeight="Bold" Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,2,0,0"/>
                    </StackPanel>
                </Border>
                <Border Background="{DynamicResource CardBackgroundBrush}" BorderBrush="{DynamicResource CardBorderBrush}" BorderThickness="1" CornerRadius="10" Padding="14,10" Margin="3,0,3,0">
                    <StackPanel>
                        <TextBlock Text="Подозрительные и рекламные" FontSize="11" FontWeight="SemiBold" Foreground="{DynamicResource TextSecondaryBrush}"/>
                        <TextBlock Text="{Binding SuspiciousCount}" FontSize="18" FontWeight="Bold" Foreground="#EF4444" Margin="0,2,0,0"/>
                    </StackPanel>
                </Border>
                <Border Background="{DynamicResource CardBackgroundBrush}" BorderBrush="{DynamicResource CardBorderBrush}" BorderThickness="1" CornerRadius="10" Padding="14,10" Margin="6,0,0,0">
                    <StackPanel>
                        <TextBlock Text="Безопасные расширения" FontSize="11" FontWeight="SemiBold" Foreground="{DynamicResource TextSecondaryBrush}"/>
                        <TextBlock Text="{Binding SafeCount}" FontSize="18" FontWeight="Bold" Foreground="#10B981" Margin="0,2,0,0"/>
                    </StackPanel>
                </Border>
            </UniformGrid>

            <!-- Filter and Search Toolbar -->
            <Border Background="{DynamicResource CardBackgroundBrush}" BorderBrush="{DynamicResource CardBorderBrush}" BorderThickness="1" CornerRadius="12" Padding="14,10" Margin="0,0,0,16">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- Filters -->
                    <WrapPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                        <!-- Browser filters -->
                        <Button Command="{Binding FilterBrowserCommand}" CommandParameter="Все" Content="Все браузеры" Style="{StaticResource StormSecondaryButton}" Padding="10,5" Margin="0,0,6,4" FontSize="11" FontWeight="Bold"/>
                        <Button Command="{Binding FilterBrowserCommand}" CommandParameter="Chrome" Content="Google Chrome" Style="{StaticResource StormSecondaryButton}" Padding="10,5" Margin="0,0,6,4" FontSize="11" FontWeight="Bold"/>
                        <Button Command="{Binding FilterBrowserCommand}" CommandParameter="Edge" Content="Microsoft Edge" Style="{StaticResource StormSecondaryButton}" Padding="10,5" Margin="0,0,6,4" FontSize="11" FontWeight="Bold"/>
                        <Button Command="{Binding FilterBrowserCommand}" CommandParameter="Яндекс" Content="Яндекс Браузер" Style="{StaticResource StormSecondaryButton}" Padding="10,5" Margin="0,0,6,4" FontSize="11" FontWeight="Bold"/>
                        <Button Command="{Binding FilterBrowserCommand}" CommandParameter="Firefox" Content="Firefox" Style="{StaticResource StormSecondaryButton}" Padding="10,5" Margin="0,0,6,4" FontSize="11" FontWeight="Bold"/>
                        <Button Command="{Binding FilterBrowserCommand}" CommandParameter="Opera" Content="Opera" Style="{StaticResource StormSecondaryButton}" Padding="10,5" Margin="0,0,14,4" FontSize="11" FontWeight="Bold"/>

                        <!-- Risk filters -->
                        <Button Command="{Binding FilterRiskCommand}" CommandParameter="Подозрительные" Content="⚠️ Подозрительные" Style="{StaticResource StormSecondaryButton}" Padding="10,5" Margin="0,0,6,4" FontSize="11" FontWeight="Bold"/>
                        <Button Command="{Binding FilterRiskCommand}" CommandParameter="Безопасные" Content="✓ Безопасные" Style="{StaticResource StormSecondaryButton}" Padding="10,5" Margin="0,0,6,4" FontSize="11" FontWeight="Bold"/>
                        <Button Command="{Binding FilterRiskCommand}" CommandParameter="Отключенные" Content="Отключенные" Style="{StaticResource StormSecondaryButton}" Padding="10,5" Margin="0,0,0,4" FontSize="11" FontWeight="Bold"/>
                    </WrapPanel>

                    <!-- Search Box -->
                    <Border Grid.Column="1" Background="{DynamicResource AppBackgroundBrush}" BorderBrush="{DynamicResource CardBorderBrush}" BorderThickness="1" CornerRadius="8" Padding="10,4" MinWidth="220" VerticalAlignment="Center">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Path Grid.Column="0" Data="{StaticResource GeoSearch}" Fill="{DynamicResource TextSecondaryBrush}" Width="11" Height="11" Stretch="Uniform" Margin="0,0,6,0" VerticalAlignment="Center"/>
                            <TextBox Grid.Column="1" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" Background="Transparent" BorderThickness="0" Foreground="{DynamicResource TextPrimaryBrush}" FontSize="11.5" VerticalAlignment="Center"/>
                        </Grid>
                    </Border>
                </Grid>
            </Border>

            <!-- Extensions List -->
            <Border Background="{DynamicResource CardBackgroundBrush}" BorderBrush="{DynamicResource CardBorderBrush}" BorderThickness="1" CornerRadius="12" Padding="16">
                <StackPanel>
                    <TextBlock Text="Установленные расширения и дополнения" FontSize="15" FontWeight="Bold" Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,0,0,12"/>

                    <ItemsControl ItemsSource="{Binding FilteredExtensions}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="{DynamicResource AppBackgroundBrush}" BorderBrush="{DynamicResource CardBorderBrush}" BorderThickness="1" CornerRadius="8" Padding="14,12" Margin="0,0,0,8">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <!-- Browser Icon -->
                                        <Border Grid.Column="0" Background="{DynamicResource CardBackgroundBrush}" BorderBrush="{DynamicResource CardBorderBrush}" BorderThickness="1" CornerRadius="8" Width="38" Height="38" Margin="0,0,12,0" VerticalAlignment="Center">
                                            <TextBlock Text="{Binding BrowserEmoji}" FontSize="18" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>

                                        <!-- Info -->
                                        <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="0,0,12,0">
                                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                                <TextBlock Text="{Binding Name}" FontSize="13.5" FontWeight="Bold" Foreground="{DynamicResource TextPrimaryBrush}"/>
                                                <Border Background="#1A00D2FF" CornerRadius="4" Padding="6,1" Margin="8,0,0,0" VerticalAlignment="Center">
                                                    <TextBlock Text="{Binding Version, StringFormat='v{0}'}" FontSize="10" FontWeight="Bold" Foreground="{DynamicResource AccentPrimaryBrush}"/>
                                                </Border>
                                                <Border Background="#1A64748B" CornerRadius="4" Padding="6,1" Margin="6,0,0,0" VerticalAlignment="Center">
                                                    <TextBlock Text="{Binding ProfileName, StringFormat='Профиль: {0}'}" FontSize="10" Foreground="{DynamicResource TextSecondaryBrush}"/>
                                                </Border>
                                            </StackPanel>
                                            <TextBlock Text="{Binding Description}" FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,2,0,2" TextWrapping="Wrap" MaxHeight="32" TextTrimming="CharacterEllipsis"/>
                                            <TextBlock Text="{Binding PermissionsSummary, StringFormat='Разрешения: {0}'}" FontSize="10" Foreground="{DynamicResource TextSecondaryBrush}" TextWrapping="Wrap"/>
                                        </StackPanel>

                                        <!-- Risk Badge -->
                                        <Border Grid.Column="2" Background="{Binding RiskBgColor}" BorderBrush="{Binding RiskBadgeColor}" BorderThickness="1" CornerRadius="5" Padding="8,3" Margin="8,0" VerticalAlignment="Center">
                                            <TextBlock Text="{Binding RiskBadge}" FontSize="10.5" FontWeight="Bold" Foreground="{Binding RiskBadgeColor}"/>
                                        </Border>

                                        <!-- Toggle Enable/Disable Button -->
                                        <Button Grid.Column="3"
                                                Command="{Binding DataContext.ToggleExtensionCommand, RelativeSource={RelativeSource AncestorType=Page}}"
                                                CommandParameter="{Binding}"
                                                Style="{StaticResource StormSecondaryButton}"
                                                Padding="12,6" Margin="0,0,6,0" VerticalAlignment="Center">
                                            <TextBlock Text="{Binding IsEnabled, Converter={StaticResource BoolToMsiButtonTextConverter}}" FontSize="11" FontWeight="Bold" Foreground="{DynamicResource TextPrimaryBrush}"/>
                                        </Button>

                                        <!-- Delete Button -->
                                        <Button Grid.Column="4"
                                                Command="{Binding DataContext.RemoveExtensionCommand, RelativeSource={RelativeSource AncestorType=Page}}"
                                                CommandParameter="{Binding}"
                                                Style="{StaticResource StormSecondaryButton}"
                                                Padding="10,6" VerticalAlignment="Center">
                                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                                <Path Data="{StaticResource GeoGarbage}" Fill="#EF4444" Width="10" Height="10" Stretch="Uniform" Margin="0,0,4,0"/>
                                                <TextBlock Text="Удалить" FontSize="11" FontWeight="Bold" Foreground="#EF4444"/>
                                            </StackPanel>
                                        </Button>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</Page>
