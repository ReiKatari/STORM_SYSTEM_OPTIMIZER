<Window x:Class="StormSystemOptimizer.Views.ResidualCleanupWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        Title="Очистка остаточных элементов"
        Width="780" MinWidth="680" MaxWidth="920"
        Height="580" MinHeight="450" MaxHeight="750"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResizeWithGrip"
        ShowInTaskbar="False"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">

    <Border Background="{DynamicResource CardBackgroundBrush}"
            BorderBrush="{DynamicResource AccentPrimaryBrush}"
            BorderThickness="1.5"
            CornerRadius="12">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="58"/>
            </Grid.RowDefinitions>

            <!-- Custom Dark Title Bar -->
            <Border Grid.Row="0" Background="{DynamicResource NavSidebarBrush}" CornerRadius="11,11,0,0" Padding="14,0" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <Path Data="{StaticResource GeoGarbage}" Fill="{DynamicResource AccentPrimaryBrush}" Width="13" Height="13" Stretch="Uniform" Margin="0,0,8,0"/>
                        <TextBlock Text="STORM — Очистка остаточных элементов программы" FontSize="12" FontWeight="Bold" Foreground="{DynamicResource AccentPrimaryBrush}"/>
                    </StackPanel>
                    <Button Click="BtnClose_Click" HorizontalAlignment="Right" Width="28" Height="28" Background="Transparent" BorderThickness="0" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="11" Cursor="Hand" Content="✕">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="bd" Background="{TemplateBinding Background}" CornerRadius="4">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="bd" Property="Background" Value="#E81123"/>
                                        <Setter Property="Foreground" Value="#FFFFFF"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </Grid>
            </Border>

            <!-- Header Info -->
            <Border Grid.Row="1" Background="{DynamicResource AppBackgroundBrush}" BorderBrush="{DynamicResource CardBorderBrush}" BorderThickness="0,0,0,1" Padding="18,14">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Border Grid.Column="0" Width="44" Height="44" CornerRadius="22" Background="#26F59E0B" BorderBrush="#F59E0B" BorderThickness="1.5" Margin="0,0,14,0">
                        <Path Data="{StaticResource GeoWarning}" Fill="#F59E0B" Width="20" Height="20" Stretch="Uniform" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>

                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                        <TextBlock x:Name="TxtAppTitle" Text="Обнаружены остаточные следы программы" FontSize="15" FontWeight="Bold" Foreground="{DynamicResource TextPrimaryBrush}"/>
                        <TextBlock x:Name="TxtSubtitle" Text="Деинсталлятор завершил работу. STORM провел глубокий анализ и выявил следующие неудаленные компоненты:" FontSize="11.5" Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,3,0,0" TextWrapping="Wrap"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Main Items Breakdown Area -->
            <Grid Grid.Row="2" Margin="18,12,18,6">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Select All Bar -->
                <Grid Grid.Row="0" Margin="0,0,0,8">
                    <CheckBox x:Name="ChkSelectAll" Content="Выбрать все обнаруженные остатки" IsChecked="True" Click="ChkSelectAll_Click" FontWeight="SemiBold" FontSize="12"/>
                    <TextBlock x:Name="TxtTotals" HorizontalAlignment="Right" FontSize="11.5" Foreground="{DynamicResource AccentPrimaryBrush}" FontWeight="Bold" VerticalAlignment="Center"/>
                </Grid>

                <!-- Scrollable breakdown -->
                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <!-- Registry Keys Section -->
                        <Border x:Name="PnlRegistrySection" Background="{DynamicResource AppBackgroundBrush}" BorderBrush="{DynamicResource CardBorderBrush}" BorderThickness="1" CornerRadius="8" Padding="12,10" Margin="0,0,0,10">
                            <StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                                    <TextBlock Text="🗝️" FontSize="13" Margin="0,0,6,0"/>
                                    <TextBlock x:Name="TxtRegistryHeader" Text="Реестр Windows" FontSize="13" FontWeight="Bold" Foreground="{DynamicResource TextPrimaryBrush}"/>
                                </StackPanel>
                                <ItemsControl x:Name="ListRegistryKeys">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay}" Click="ItemCheck_Click" Margin="0,3,0,3">
                                                <TextBlock Text="{Binding Path}" FontSize="11.5" Foreground="{DynamicResource TextPrimaryBrush}" TextWrapping="Wrap"/>
                                            </CheckBox>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </Border>

                        <!-- Folders Section -->
                        <Border x:Name="PnlFoldersSection" Background="{DynamicResource AppBackgroundBrush}" BorderBrush="{DynamicResource CardBorderBrush}" BorderThickness="1" CornerRadius="8" Padding="12,10">
                            <StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                                    <TextBlock Text="📁" FontSize="13" Margin="0,0,6,0"/>
                                    <TextBlock x:Name="TxtFoldersHeader" Text="Файлы и папки на диске" FontSize="13" FontWeight="Bold" Foreground="{DynamicResource TextPrimaryBrush}"/>
                                </StackPanel>
                                <ItemsControl x:Name="ListFolders">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay}" Click="ItemCheck_Click" Margin="0,3,0,3">
                                                <TextBlock Text="{Binding Path}" FontSize="11.5" Foreground="{DynamicResource TextPrimaryBrush}" TextWrapping="Wrap"/>
                                            </CheckBox>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </ScrollViewer>
            </Grid>

            <!-- Bottom Action Footer -->
            <Border Grid.Row="3" Background="{DynamicResource NavSidebarBrush}" CornerRadius="0,0,11,11" Padding="18,10">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock x:Name="TxtSelectedCount" Text="Выбрано: 0 элементов" FontSize="12" Foreground="{DynamicResource TextSecondaryBrush}" VerticalAlignment="Center"/>

                    <StackPanel Grid.Column="1" Orientation="Horizontal">
                        <Button Click="BtnSkip_Click" Style="{StaticResource StormSecondaryButton}" Padding="16,8" Margin="0,0,10,0">
                            <TextBlock Text="Пропустить" FontSize="12" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryBrush}"/>
                        </Button>
                        <Button x:Name="BtnClean" Click="BtnClean_Click" Style="{StaticResource StormHeroButton}" Padding="18,8">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <Path Data="{StaticResource GeoGarbage}" Fill="#FFFFFF" Width="12" Height="12" Stretch="Uniform" Margin="0,0,6,0"/>
                                <TextBlock Text="Очистить остаточные следы" FontSize="12" FontWeight="Bold" Foreground="#FFFFFF"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>