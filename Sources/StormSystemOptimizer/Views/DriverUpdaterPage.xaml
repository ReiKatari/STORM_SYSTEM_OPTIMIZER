<Page x:Class="StormSystemOptimizer.Views.DriverUpdaterPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:vm="clr-namespace:StormSystemOptimizer.ViewModels"
      mc:Ignorable="d"
      d:DesignHeight="850" d:DesignWidth="1100"
      Title="Обновление драйверов">

    <Page.DataContext>
        <vm:DriverUpdaterViewModel/>
    </Page.DataContext>

    <Grid Background="{DynamicResource AppBackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header Section -->
        <Border Grid.Row="0" Background="{DynamicResource CardBackgroundBrush}" BorderBrush="{DynamicResource CardBorderBrush}" BorderThickness="1" CornerRadius="12" Padding="20" Margin="20,20,20,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                        <Border Background="#1A38BDF8" BorderBrush="#3338BDF8" BorderThickness="1" CornerRadius="8" Padding="6" Margin="0,0,10,0" VerticalAlignment="Center">
                            <Path Data="{StaticResource GeoDevice}" Fill="{StaticResource IconGradSky}" Width="16" Height="16" Stretch="Uniform"/>
                        </Border>
                        <TextBlock Text="Центр обновления драйверов" FontSize="18" FontWeight="Bold" Foreground="{DynamicResource TextPrimaryBrush}" VerticalAlignment="Center"/>
                    </StackPanel>
                    <TextBlock Text="Проверка актуальности драйверов видеокарты, сети, чипсета и звука. Резервное копирование и безопасная установка с созданием точек восстановления."
                               FontSize="12" Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,0,0,8"/>
                    <TextBlock Text="{Binding StatsSummary}" FontSize="12" FontWeight="SemiBold" Foreground="{DynamicResource AccentPrimaryBrush}"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button Command="{Binding CreateSystemRestorePointCommand}" Style="{StaticResource StormSecondaryButton}" Padding="12,8" Margin="0,0,8,0">
                        <StackPanel Orientation="Horizontal">
                            <Path Data="{StaticResource GeoShield}" Fill="{StaticResource AccentPrimaryBrush}" Width="12" Height="12" Stretch="Uniform" Margin="0,0,6,0"/>
                            <TextBlock Text="🛡️ Создать бэкап" FontSize="11.5" FontWeight="Bold" Foreground="{DynamicResource TextPrimaryBrush}"/>
                        </StackPanel>
                    </Button>

                    <Button Command="{Binding BackupAllDriversCommand}" Style="{StaticResource StormSecondaryButton}" Padding="12,8" Margin="0,0,8,0">
                        <TextBlock Text="💾 Экспорт драйверов" FontSize="11.5" FontWeight="Bold" Foreground="{DynamicResource TextPrimaryBrush}"/>
                    </Button>

                    <Button Command="{Binding LoadDriversCommand}" Style="{StaticResource StormHeroButton}" Padding="14,8">
                        <TextBlock Text="🔄 Сканировать" FontSize="12" FontWeight="Bold" Foreground="#FFFFFF"/>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Categories Filter Bar -->
        <Border Grid.Row="1" Background="{DynamicResource CardBackgroundBrush}" BorderBrush="{DynamicResource CardBorderBrush}" BorderThickness="1" CornerRadius="10" Padding="14,10" Margin="20,0,20,12">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <RadioButton Content="Все устройства" IsChecked="True" Style="{StaticResource NavRadioButtonStyle}" Click="CatAll_Click" Margin="0,0,8,0" Padding="12,6"/>
                <RadioButton Content="🎮 Видеокарта" Style="{StaticResource NavRadioButtonStyle}" Click="CatGpu_Click" Margin="0,0,8,0" Padding="12,6"/>
                <RadioButton Content="🌐 Сеть и Wi-Fi" Style="{StaticResource NavRadioButtonStyle}" Click="CatNet_Click" Margin="0,0,8,0" Padding="12,6"/>
                <RadioButton Content="🔊 Звук" Style="{StaticResource NavRadioButtonStyle}" Click="CatAudio_Click" Margin="0,0,8,0" Padding="12,6"/>
                <RadioButton Content="💾 Накопители" Style="{StaticResource NavRadioButtonStyle}" Click="CatDisk_Click" Margin="0,0,8,0" Padding="12,6"/>
                <RadioButton Content="⚡ Чипсет &amp; USB" Style="{StaticResource NavRadioButtonStyle}" Click="CatChipset_Click" Margin="0,0,8,0" Padding="12,6"/>
            </StackPanel>
        </Border>

        <!-- Drivers List -->
        <Border Grid.Row="2" Background="{DynamicResource CardBackgroundBrush}" BorderBrush="{DynamicResource CardBorderBrush}" BorderThickness="1" CornerRadius="12" Padding="12" Margin="20,0,20,12">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding DisplayDrivers}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="{DynamicResource AppBackgroundBrush}" BorderBrush="{DynamicResource CardBorderBrush}" BorderThickness="1" CornerRadius="10" Padding="16,12" Margin="0,0,0,8">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="190"/>
                                        <ColumnDefinition Width="190"/>
                                        <ColumnDefinition Width="130"/>
                                    </Grid.ColumnDefinitions>

                                    <!-- Device Name & Provider -->
                                    <StackPanel Grid.Column="0" VerticalAlignment="Center" Margin="0,0,16,0">
                                        <TextBlock Text="{Binding DeviceName}" FontSize="13.5" FontWeight="Bold" Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,0,0,3" TextWrapping="Wrap"/>
                                        <TextBlock Text="{Binding ProviderName, StringFormat='{}Поставщик: {0}'}" FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}" TextWrapping="Wrap"/>
                                    </StackPanel>

                                    <!-- Installed Version & Date -->
                                    <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="0,0,12,0">
                                        <TextBlock Text="{Binding CurrentVersion, StringFormat='{}Установлен: {0}'}" FontSize="11.5" FontWeight="Bold" Foreground="{DynamicResource TextPrimaryBrush}"/>
                                        <TextBlock Text="{Binding DriverDate, StringFormat='{}Дата релиза: {0}'}" FontSize="10.5" Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,2,0,0"/>
                                    </StackPanel>

                                    <!-- Latest Version -->
                                    <StackPanel Grid.Column="2" VerticalAlignment="Center" Margin="0,0,12,0">
                                        <TextBlock Text="{Binding LatestVersion, StringFormat='{}Доступен: {0}'}" FontSize="11.5" FontWeight="SemiBold" Foreground="{DynamicResource AccentPrimaryBrush}"/>
                                        <TextBlock Text="Сертификация WHQL" FontSize="10.5" Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,2,0,0"/>
                                    </StackPanel>

                                    <!-- Action Button or Locked Actual Badge -->
                                    <StackPanel Grid.Column="3" VerticalAlignment="Center">
                                        <!-- When Update Available -->
                                        <Button Visibility="{Binding IsUpdateAvailable, Converter={StaticResource BoolToVisConverter}}"
                                                Command="{Binding DataContext.UpdateDriverCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                CommandParameter="{Binding}"
                                                Style="{StaticResource StormHeroButton}"
                                                Padding="14,8">
                                            <StackPanel Orientation="Horizontal">
                                                <Path Data="{StaticResource GeoLog}" Fill="#FFFFFF" Width="11" Height="11" Stretch="Uniform" Margin="0,0,6,0"/>
                                                <TextBlock Text="⚡ Обновить" FontSize="11.5" FontWeight="Bold" Foreground="#FFFFFF"/>
                                            </StackPanel>
                                        </Button>

                                        <!-- When Driver is Up to Date -->
                                        <Border Visibility="{Binding IsUpdateAvailable, Converter={StaticResource InverseBoolToVisConverter}}"
                                                Background="#2610B981"
                                                BorderBrush="#10B981"
                                                BorderThickness="1"
                                                CornerRadius="8"
                                                Padding="14,8"
                                                ToolTip="Установлен актуальный сертифицированный драйвер WHQL">
                                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                                <Path Data="{StaticResource GeoCheck}" Fill="#10B981" Width="11" Height="11" Stretch="Uniform" Margin="0,0,6,0" VerticalAlignment="Center"/>
                                                <TextBlock Text="Актуален" FontSize="11.5" FontWeight="Bold" Foreground="#10B981" VerticalAlignment="Center"/>
                                            </StackPanel>
                                        </Border>
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>

        <!-- FAQ Section with Gradient Vector Icons -->
        <Border Grid.Row="3" Background="{DynamicResource CardBackgroundBrush}" BorderBrush="{DynamicResource CardBorderBrush}" BorderThickness="1" CornerRadius="10" Padding="14,10" Margin="20,0,20,10">
            <StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                    <Border Background="#1A00D2FF" BorderBrush="#3300D2FF" BorderThickness="1" CornerRadius="4" Width="18" Height="18" Margin="0,0,8,0" VerticalAlignment="Center">
                        <Path Data="{StaticResource GeoQuestion}" Fill="{StaticResource IconGradCyan}" Width="10" Height="10" Stretch="Uniform" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <TextBlock Text="Частые вопросы по Обновлению драйверов (FAQ)" FontSize="12.5" FontWeight="Bold" Foreground="{DynamicResource TextPrimaryBrush}" VerticalAlignment="Center"/>
                </StackPanel>
                <Expander Foreground="{DynamicResource TextPrimaryBrush}" FontSize="12" FontWeight="SemiBold">
                    <Expander.Header>
                        <StackPanel Orientation="Horizontal">
                            <Path Data="{StaticResource GeoShield}" Fill="{StaticResource IconGradEmerald}" Width="12" Height="12" Stretch="Uniform" Margin="0,0,8,0" VerticalAlignment="Center"/>
                            <TextBlock Text="Что означает статус «Актуален» и сертификат WHQL?" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Expander.Header>
                    <TextBlock Text="Статус «Актуален» означает, что для оборудования установлена новейшая версия драйвера. Сертификация Microsoft WHQL гарантирует 100% стабильность работы и отсутствие сбоев BSOD." FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}" Margin="10,2,0,2" TextWrapping="Wrap"/>
                </Expander>
            </StackPanel>
        </Border>

        <!-- Status Bottom Banner -->
        <Border Grid.Row="4" Background="{DynamicResource CardBackgroundBrush}" BorderBrush="{DynamicResource CardBorderBrush}" BorderThickness="1" CornerRadius="8" Padding="14,8" Margin="20,0,20,20">
            <TextBlock Text="{Binding StatusText}" FontSize="11.5" FontWeight="SemiBold" Foreground="{DynamicResource AccentPrimaryBrush}"/>
        </Border>
    </Grid>
</Page>
