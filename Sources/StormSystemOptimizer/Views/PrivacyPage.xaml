<Page x:Class="StormSystemOptimizer.Views.PrivacyPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:vm="clr-namespace:StormSystemOptimizer.ViewModels"
      Title="Privacy">

    <Page.DataContext>
        <vm:PrivacyViewModel x:Name="ViewModel"/>
    </Page.DataContext>

    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="20">
        <StackPanel HorizontalAlignment="Stretch">

            <!-- Top Header & Profiles Card -->
            <Border Background="{DynamicResource CardBackgroundBrush}" BorderBrush="{DynamicResource CardBorderBrush}" BorderThickness="1" CornerRadius="12" Padding="20" Margin="0,0,0,16">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Margin="0,0,16,0">
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                            <Border Background="#1AFB7185" BorderBrush="#33FB7185" BorderThickness="1" CornerRadius="6" Width="26" Height="26" Margin="0,0,8,0" VerticalAlignment="Center">
                                <Path Data="{StaticResource GeoPrivacy}" Fill="{StaticResource IconGradRose}" Width="14" Height="14" Stretch="Uniform" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <TextBlock Text="Конфиденциальность и защита от телеметрии" FontSize="20" FontWeight="Bold" Foreground="{DynamicResource TextPrimaryBrush}" VerticalAlignment="Center"/>
                        </StackPanel>
                        <TextBlock Text="Полное отключение скрытой телеметрии Microsoft, трекеров рекламы, фонового кейлоггинга и облачного сбора личных данных."
                                   FontSize="13" Foreground="{DynamicResource TextSecondaryBrush}" TextWrapping="Wrap" Margin="0,0,0,10"/>
                        <Border Background="{DynamicResource AppBackgroundBrush}" BorderBrush="{DynamicResource CardBorderBrush}" BorderThickness="1" CornerRadius="6" Padding="12,6" HorizontalAlignment="Left">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="Статус: " FontSize="12" Foreground="{DynamicResource TextSecondaryBrush}"/>
                                <TextBlock Text="{Binding StatusMessage}" FontSize="12" FontWeight="Bold" Foreground="{DynamicResource AccentPrimaryBrush}" TextWrapping="Wrap"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>

                    <!-- Presets & Apply Button -->
                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,8" HorizontalAlignment="Right">
                            <Button Command="{Binding ApplyPresetCommand}" CommandParameter="Max" Style="{StaticResource StormHeroButton}" Padding="12,7" Margin="0,0,6,0">
                                <Grid Margin="0,0,0,2">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Border Grid.Column="0" Background="#33FFFFFF" CornerRadius="3" Padding="2" Margin="0,2,6,0" VerticalAlignment="Top">
                                    <Path Data="{StaticResource GeoShield}" Fill="#FFFFFF" Width="10" Height="10" Stretch="Uniform"/>
                                </Border>
                                <TextBlock Grid.Column="1" Text="Максимальная" FontSize="11" FontWeight="Bold" Foreground="#FFFFFF" TextWrapping="Wrap" VerticalAlignment="Center"/>
                            </Grid>
                            </Button>

                            <Button Command="{Binding ApplyPresetCommand}" CommandParameter="Balanced" Style="{StaticResource StormSecondaryButton}" Padding="12,7" Margin="0,0,6,0">
                                <Grid Margin="0,0,0,2">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Border Grid.Column="0" Background="#1A34D399" CornerRadius="3" Padding="2" Margin="0,2,6,0" VerticalAlignment="Top">
                                    <Path Data="{StaticResource GeoCheck}" Fill="{StaticResource IconGradEmerald}" Width="10" Height="10" Stretch="Uniform"/>
                                </Border>
                                <TextBlock Grid.Column="1" Text="Базовая" FontSize="11" FontWeight="Bold" Foreground="{DynamicResource TextPrimaryBrush}" TextWrapping="Wrap" VerticalAlignment="Center"/>
                            </Grid>
                            </Button>

                            <Button Command="{Binding ApplyPresetCommand}" CommandParameter="Default" Style="{StaticResource StormSecondaryButton}" Padding="12,7">
                                <Grid Margin="0,0,0,2">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Border Grid.Column="0" Background="#1A00D2FF" CornerRadius="3" Padding="2" Margin="0,2,6,0" VerticalAlignment="Top">
                                    <Path Data="{StaticResource GeoScanner}" Fill="{StaticResource IconGradCyan}" Width="10" Height="10" Stretch="Uniform"/>
                                </Border>
                                <TextBlock Grid.Column="1" Text="Сброс" FontSize="11" FontWeight="Bold" Foreground="{DynamicResource TextPrimaryBrush}" TextWrapping="Wrap" VerticalAlignment="Center"/>
                            </Grid>
                            </Button>
                        </StackPanel>

                        <Button Command="{Binding ApplyPrivacySettingsCommand}"
                                Style="{StaticResource StormHeroButton}"
                                HorizontalAlignment="Right"
                                Padding="16,8">
                            <Grid Margin="0,0,0,2">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Border Grid.Column="0" Background="#33FFFFFF" CornerRadius="3" Padding="2" Margin="0,2,6,0" VerticalAlignment="Top">
                                    <Path Data="{StaticResource GeoDisks}" Fill="#FFFFFF" Width="10" Height="10" Stretch="Uniform"/>
                                </Border>
                                <TextBlock Grid.Column="1" Text="Применить выбранные настройки" FontSize="12" FontWeight="Bold" Foreground="#FFFFFF" TextWrapping="Wrap" VerticalAlignment="Center"/>
                            </Grid>
                        </Button>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Group 1: Диагностика и Телеметрия -->
            <StackPanel Orientation="Horizontal" Margin="0,4,0,8">
                <Border Background="#1A00D2FF" BorderBrush="#3300D2FF" BorderThickness="1" CornerRadius="5" Width="20" Height="20" Margin="0,0,6,0" VerticalAlignment="Center">
                    <Path Data="{StaticResource GeoDashboard}" Fill="{StaticResource IconGradCyan}" Width="10" Height="10" Stretch="Uniform" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>
                <TextBlock Text="Диагностика и системная телеметрия" FontSize="15" FontWeight="Bold" Foreground="{DynamicResource TextPrimaryBrush}" VerticalAlignment="Center"/>
            </StackPanel>

            <Border Background="{DynamicResource CardBackgroundBrush}" BorderBrush="{DynamicResource CardBorderBrush}" BorderThickness="1" CornerRadius="12" Padding="16" Margin="0,0,0,14">
                <StackPanel>
                    <!-- 1. Diagnostic Data -->
                    <Border Background="{DynamicResource AppBackgroundBrush}" CornerRadius="8" Padding="12" Margin="0,0,0,8">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <CheckBox x:Name="Chk1" Grid.Column="0" IsChecked="{Binding IsTelemetryDisabled, Mode=TwoWay}" VerticalAlignment="Center" Margin="0,0,12,0"/>
                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                <TextBlock Text="Диагностическая телеметрия Windows" FontSize="13" FontWeight="Bold" Foreground="{DynamicResource TextPrimaryBrush}"/>
                                <TextBlock Text="Запретить сбор и отправку логов производительности, дампов сбоев и сведений об оборудовании на сервера Microsoft." FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}" TextWrapping="Wrap" Margin="0,2,10,0"/>
                            </StackPanel>
                            <Border Grid.Column="2" CornerRadius="6" Padding="10,5" VerticalAlignment="Center" MinWidth="130">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Setter Property="Background" Value="#1A10B981"/>
                                        <Setter Property="BorderBrush" Value="#3310B981"/>
                                        <Setter Property="BorderThickness" Value="1"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding ElementName=Chk1, Path=IsChecked}" Value="False">
                                                <Setter Property="Background" Value="#1AF59E0B"/>
                                                <Setter Property="BorderBrush" Value="#33F59E0B"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <TextBlock HorizontalAlignment="Center" FontSize="10.5" FontWeight="Bold">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Text" Value="🛡️ ЗАЩИЩЕНО"/>
                                            <Setter Property="Foreground" Value="#10B981"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding ElementName=Chk1, Path=IsChecked}" Value="False">
                                                    <Setter Property="Text" Value="⚠️ СЛЕЖКА АКТИВНА"/>
                                                    <Setter Property="Foreground" Value="#F59E0B"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </Border>
                        </Grid>
                    </Border>

                    <!-- 2. Error Reporting -->
                    <Border Background="{DynamicResource AppBackgroundBrush}" CornerRadius="8" Padding="12" Margin="0,0,0,8">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <CheckBox x:Name="Chk2" Grid.Column="0" IsChecked="{Binding IsErrorReportingDisabled, Mode=TwoWay}" VerticalAlignment="Center" Margin="0,0,12,0"/>
                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                <TextBlock Text="Отчеты об ошибках Windows (Windows Error Reporting)" FontSize="13" FontWeight="Bold" Foreground="{DynamicResource TextPrimaryBrush}"/>
                                <TextBlock Text="Отключить автоматическую передачу системных дампов памяти и аварийных дампов сторонних программ." FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}" TextWrapping="Wrap" Margin="0,2,10,0"/>
                            </StackPanel>
                            <Border Grid.Column="2" CornerRadius="6" Padding="10,5" VerticalAlignment="Center" MinWidth="130">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Setter Property="Background" Value="#1A10B981"/>
                                        <Setter Property="BorderBrush" Value="#3310B981"/>
                                        <Setter Property="BorderThickness" Value="1"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding ElementName=Chk2, Path=IsChecked}" Value="False">
                                                <Setter Property="Background" Value="#1AF59E0B"/>
                                                <Setter Property="BorderBrush" Value="#33F59E0B"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <TextBlock HorizontalAlignment="Center" FontSize="10.5" FontWeight="Bold">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Text" Value="🛡️ ЗАЩИЩЕНО"/>
                                            <Setter Property="Foreground" Value="#10B981"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding ElementName=Chk2, Path=IsChecked}" Value="False">
                                                    <Setter Property="Text" Value="⚠️ СЛЕЖКА АКТИВНА"/>
                                                    <Setter Property="Foreground" Value="#F59E0B"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </Border>
                        </Grid>
                    </Border>

                    <!-- 3. App Compatibility Inventory -->
                    <Border Background="{DynamicResource AppBackgroundBrush}" CornerRadius="8" Padding="12" Margin="0,0,0,8">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <CheckBox x:Name="Chk3" Grid.Column="0" IsChecked="{Binding IsAppInventoryDisabled, Mode=TwoWay}" VerticalAlignment="Center" Margin="0,0,12,0"/>
                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                <TextBlock Text="Телеметрия инвентаря приложений и совместимости" FontSize="13" FontWeight="Bold" Foreground="{DynamicResource TextPrimaryBrush}"/>
                                <TextBlock Text="Запретить периодическое фоновое сканирование списка установленных программ, игр и файлов службой совместимости." FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}" TextWrapping="Wrap" Margin="0,2,10,0"/>
                            </StackPanel>
                            <Border Grid.Column="2" CornerRadius="6" Padding="10,5" VerticalAlignment="Center" MinWidth="130">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Setter Property="Background" Value="#1A10B981"/>
                                        <Setter Property="BorderBrush" Value="#3310B981"/>
                                        <Setter Property="BorderThickness" Value="1"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding ElementName=Chk3, Path=IsChecked}" Value="False">
                                                <Setter Property="Background" Value="#1AF59E0B"/>
                                                <Setter Property="BorderBrush" Value="#33F59E0B"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <TextBlock HorizontalAlignment="Center" FontSize="10.5" FontWeight="Bold">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Text" Value="🛡️ ЗАЩИЩЕНО"/>
                                            <Setter Property="Foreground" Value="#10B981"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding ElementName=Chk3, Path=IsChecked}" Value="False">
                                                    <Setter Property="Text" Value="⚠️ СЛЕЖКА АКТИВНА"/>
                                                    <Setter Property="Foreground" Value="#F59E0B"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </Border>
                        </Grid>
                    </Border>

                    <!-- 4. Hosts Blocker -->
                    <Border Background="{DynamicResource AppBackgroundBrush}" CornerRadius="8" Padding="12" Margin="0,0,0,8">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <CheckBox x:Name="ChkHosts" Grid.Column="0" IsChecked="{Binding IsHostsBlockEnabled, Mode=TwoWay}" VerticalAlignment="Center" Margin="0,0,12,0"/>
                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                <TextBlock Text="Блокировка серверов телеметрии через файл хостов" FontSize="13" FontWeight="Bold" Foreground="{DynamicResource TextPrimaryBrush}"/>
                                <TextBlock Text="Перенаправление более 1400 серверов сбора данных, трекеров и шпионских шлюзов на локальный адрес без доступа в интернет." FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}" TextWrapping="Wrap" Margin="0,2,10,0"/>
                            </StackPanel>
                            <Border Grid.Column="2" CornerRadius="6" Padding="10,5" VerticalAlignment="Center" MinWidth="130">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Setter Property="Background" Value="#1A10B981"/>
                                        <Setter Property="BorderBrush" Value="#3310B981"/>
                                        <Setter Property="BorderThickness" Value="1"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding ElementName=ChkHosts, Path=IsChecked}" Value="False">
                                                <Setter Property="Background" Value="#1AF59E0B"/>
                                                <Setter Property="BorderBrush" Value="#33F59E0B"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <TextBlock HorizontalAlignment="Center" FontSize="10.5" FontWeight="Bold">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Text" Value="🛡️ ЗАЩИЩЕНО"/>
                                            <Setter Property="Foreground" Value="#10B981"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding ElementName=ChkHosts, Path=IsChecked}" Value="False">
                                                    <Setter Property="Text" Value="⚠️ СЛЕЖКА АКТИВНА"/>
                                                    <Setter Property="Foreground" Value="#F59E0B"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </Border>
                        </Grid>
                    </Border>

                    <!-- 5. Firewall Blocker -->
                    <Border Background="{DynamicResource AppBackgroundBrush}" CornerRadius="8" Padding="12" Margin="0,0,0,8">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <CheckBox x:Name="ChkFw" Grid.Column="0" IsChecked="{Binding IsFirewallBlockEnabled, Mode=TwoWay}" VerticalAlignment="Center" Margin="0,0,12,0"/>
                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                <TextBlock Text="Блокировка серверов телеметрии через брандмауэр" FontSize="13" FontWeight="Bold" Foreground="{DynamicResource TextPrimaryBrush}"/>
                                <TextBlock Text="Создание запрещающих правил сетевого экрана для исходящих подключений системных процессов сбора статистики." FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}" TextWrapping="Wrap" Margin="0,2,10,0"/>
                            </StackPanel>
                            <Border Grid.Column="2" CornerRadius="6" Padding="10,5" VerticalAlignment="Center" MinWidth="130">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Setter Property="Background" Value="#1A10B981"/>
                                        <Setter Property="BorderBrush" Value="#3310B981"/>
                                        <Setter Property="BorderThickness" Value="1"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding ElementName=ChkFw, Path=IsChecked}" Value="False">
                                                <Setter Property="Background" Value="#1AF59E0B"/>
                                                <Setter Property="BorderBrush" Value="#33F59E0B"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <TextBlock HorizontalAlignment="Center" FontSize="10.5" FontWeight="Bold">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Text" Value="🛡️ ЗАЩИЩЕНО"/>
                                            <Setter Property="Foreground" Value="#10B981"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding ElementName=ChkFw, Path=IsChecked}" Value="False">
                                                    <Setter Property="Text" Value="⚠️ СЛЕЖКА АКТИВНА"/>
                                                    <Setter Property="Foreground" Value="#F59E0B"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </Border>
                        </Grid>
                    </Border>

                    <!-- 6. GPU Telemetry -->
                    <Border Background="{DynamicResource AppBackgroundBrush}" CornerRadius="8" Padding="12" Margin="0,0,0,8">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <CheckBox x:Name="ChkGpu" Grid.Column="0" IsChecked="{Binding IsNvidiaTelemetryDisabled, Mode=TwoWay}" VerticalAlignment="Center" Margin="0,0,12,0"/>
                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                <TextBlock Text="Отключение сбора телеметрии видеокарт" FontSize="13" FontWeight="Bold" Foreground="{DynamicResource TextPrimaryBrush}"/>
                                <TextBlock Text="Деактивация фоновых служб и контейнеров отслеживания действий пользователя в видеодрайвере." FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}" TextWrapping="Wrap" Margin="0,2,10,0"/>
                            </StackPanel>
                            <Border Grid.Column="2" CornerRadius="6" Padding="10,5" VerticalAlignment="Center" MinWidth="130">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Setter Property="Background" Value="#1A10B981"/>
                                        <Setter Property="BorderBrush" Value="#3310B981"/>
                                        <Setter Property="BorderThickness" Value="1"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding ElementName=ChkGpu, Path=IsChecked}" Value="False">
                                                <Setter Property="Background" Value="#1AF59E0B"/>
                                                <Setter Property="BorderBrush" Value="#33F59E0B"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <TextBlock HorizontalAlignment="Center" FontSize="10.5" FontWeight="Bold">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Text" Value="🛡️ ЗАЩИЩЕНО"/>
                                            <Setter Property="Foreground" Value="#10B981"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding ElementName=ChkGpu, Path=IsChecked}" Value="False">
                                                    <Setter Property="Text" Value="⚠️ СЛЕЖКА АКТИВНА"/>
                                                    <Setter Property="Foreground" Value="#F59E0B"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </Border>
                        </Grid>
                    </Border>

                    <!-- 7. CPU Telemetry -->
                    <Border Background="{DynamicResource AppBackgroundBrush}" CornerRadius="8" Padding="12">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <CheckBox x:Name="ChkCpu" Grid.Column="0" IsChecked="{Binding IsIntelTelemetryDisabled, Mode=TwoWay}" VerticalAlignment="Center" Margin="0,0,12,0"/>
                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                <TextBlock Text="Отключение сбора телеметрии процессоров" FontSize="13" FontWeight="Bold" Foreground="{DynamicResource TextPrimaryBrush}"/>
                                <TextBlock Text="Отключение фоновых служб отправки статистики использования оборудования и отчетов процессора." FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}" TextWrapping="Wrap" Margin="0,2,10,0"/>
                            </StackPanel>
                            <Border Grid.Column="2" CornerRadius="6" Padding="10,5" VerticalAlignment="Center" MinWidth="130">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Setter Property="Background" Value="#1A10B981"/>
                                        <Setter Property="BorderBrush" Value="#3310B981"/>
                                        <Setter Property="BorderThickness" Value="1"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding ElementName=ChkCpu, Path=IsChecked}" Value="False">
                                                <Setter Property="Background" Value="#1AF59E0B"/>
                                                <Setter Property="BorderBrush" Value="#33F59E0B"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <TextBlock HorizontalAlignment="Center" FontSize="10.5" FontWeight="Bold">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Text" Value="🛡️ ЗАЩИЩЕНО"/>
                                            <Setter Property="Foreground" Value="#10B981"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding ElementName=ChkCpu, Path=IsChecked}" Value="False">
                                                    <Setter Property="Text" Value="⚠️ СЛЕЖКА АКТИВНА"/>
                                                    <Setter Property="Foreground" Value="#F59E0B"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </Border>
                        </Grid>
                    </Border>
                </StackPanel>
            </Border>

            <!-- Group 2: Трекинг, реклама и облако -->
            <StackPanel Orientation="Horizontal" Margin="0,4,0,8">
                <Border Background="#1AC084FC" BorderBrush="#33C084FC" BorderThickness="1" CornerRadius="5" Width="20" Height="20" Margin="0,0,6,0" VerticalAlignment="Center">
                    <Path Data="{StaticResource GeoScanner}" Fill="{StaticResource IconGradPurple}" Width="10" Height="10" Stretch="Uniform" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>
                <TextBlock Text="Рекламные трекеры и облачный сбор активности" FontSize="15" FontWeight="Bold" Foreground="{DynamicResource TextPrimaryBrush}" VerticalAlignment="Center"/>
            </StackPanel>

            <Border Background="{DynamicResource CardBackgroundBrush}" BorderBrush="{DynamicResource CardBorderBrush}" BorderThickness="1" CornerRadius="12" Padding="16" Margin="0,0,0,14">
                <StackPanel>
                    <!-- 4. Advertising ID -->
                    <Border Background="{DynamicResource AppBackgroundBrush}" CornerRadius="8" Padding="12" Margin="0,0,0,8">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <CheckBox x:Name="Chk4" Grid.Column="0" IsChecked="{Binding IsAdIdDisabled, Mode=TwoWay}" VerticalAlignment="Center" Margin="0,0,12,0"/>
                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                <TextBlock Text="Рекламный идентификатор (Advertising ID)" FontSize="13" FontWeight="Bold" Foreground="{DynamicResource TextPrimaryBrush}"/>
                                <TextBlock Text="Запретить приложениям и сайтам использовать уникальный идентификатор для отслеживания интересов и показа рекламы." FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}" TextWrapping="Wrap" Margin="0,2,10,0"/>
                            </StackPanel>
                            <Border Grid.Column="2" CornerRadius="6" Padding="10,5" VerticalAlignment="Center" MinWidth="130">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Setter Property="Background" Value="#1A10B981"/>
                                        <Setter Property="BorderBrush" Value="#3310B981"/>
                                        <Setter Property="BorderThickness" Value="1"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding ElementName=Chk4, Path=IsChecked}" Value="False">
                                                <Setter Property="Background" Value="#1AF59E0B"/>
                                                <Setter Property="BorderBrush" Value="#33F59E0B"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <TextBlock HorizontalAlignment="Center" FontSize="10.5" FontWeight="Bold">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Text" Value="🛡️ ЗАЩИЩЕНО"/>
                                            <Setter Property="Foreground" Value="#10B981"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding ElementName=Chk4, Path=IsChecked}" Value="False">
                                                    <Setter Property="Text" Value="⚠️ СЛЕЖКА АКТИВНА"/>
                                                    <Setter Property="Foreground" Value="#F59E0B"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </Border>
                        </Grid>
                    </Border>

                    <!-- 5. Activity History -->
                    <Border Background="{DynamicResource AppBackgroundBrush}" CornerRadius="8" Padding="12" Margin="0,0,0,8">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <CheckBox x:Name="Chk5" Grid.Column="0" IsChecked="{Binding IsActivityFeedDisabled, Mode=TwoWay}" VerticalAlignment="Center" Margin="0,0,12,0"/>
                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                <TextBlock Text="История активности и облачная синхронизация" FontSize="13" FontWeight="Bold" Foreground="{DynamicResource TextPrimaryBrush}"/>
                                <TextBlock Text="Отключить сбор сведений об открытых файлах, веб-страницах и приложениях в ленту активности Microsoft Timeline." FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}" TextWrapping="Wrap" Margin="0,2,10,0"/>
                            </StackPanel>
                            <Border Grid.Column="2" CornerRadius="6" Padding="10,5" VerticalAlignment="Center" MinWidth="130">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Setter Property="Background" Value="#1A10B981"/>
                                        <Setter Property="BorderBrush" Value="#3310B981"/>
                                        <Setter Property="BorderThickness" Value="1"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding ElementName=Chk5, Path=IsChecked}" Value="False">
                                                <Setter Property="Background" Value="#1AF59E0B"/>
                                                <Setter Property="BorderBrush" Value="#33F59E0B"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <TextBlock HorizontalAlignment="Center" FontSize="10.5" FontWeight="Bold">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Text" Value="🛡️ ЗАЩИЩЕНО"/>
                                            <Setter Property="Foreground" Value="#10B981"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding ElementName=Chk5, Path=IsChecked}" Value="False">
                                                    <Setter Property="Text" Value="⚠️ СЛЕЖКА АКТИВНА"/>
                                                    <Setter Property="Foreground" Value="#F59E0B"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </Border>
                        </Grid>
                    </Border>

                    <!-- 6. Input Telemetry / Keylogging -->
                    <Border Background="{DynamicResource AppBackgroundBrush}" CornerRadius="8" Padding="12">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <CheckBox x:Name="Chk6" Grid.Column="0" IsChecked="{Binding IsInputTelemetryDisabled, Mode=TwoWay}" VerticalAlignment="Center" Margin="0,0,12,0"/>
                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                <TextBlock Text="Сбор рукописного ввода, набора текста и клавиатурный монитор" FontSize="13" FontWeight="Bold" Foreground="{DynamicResource TextPrimaryBrush}"/>
                                <TextBlock Text="Заблокировать отправку введенного текста, словарей автоисправления и речевых шаблонов на сервера Microsoft." FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}" TextWrapping="Wrap" Margin="0,2,10,0"/>
                            </StackPanel>
                            <Border Grid.Column="2" CornerRadius="6" Padding="10,5" VerticalAlignment="Center" MinWidth="130">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Setter Property="Background" Value="#1A10B981"/>
                                        <Setter Property="BorderBrush" Value="#3310B981"/>
                                        <Setter Property="BorderThickness" Value="1"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding ElementName=Chk6, Path=IsChecked}" Value="False">
                                                <Setter Property="Background" Value="#1AF59E0B"/>
                                                <Setter Property="BorderBrush" Value="#33F59E0B"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <TextBlock HorizontalAlignment="Center" FontSize="10.5" FontWeight="Bold">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Text" Value="🛡️ ЗАЩИЩЕНО"/>
                                            <Setter Property="Foreground" Value="#10B981"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding ElementName=Chk6, Path=IsChecked}" Value="False">
                                                    <Setter Property="Text" Value="⚠️ СЛЕЖКА АКТИВНА"/>
                                                    <Setter Property="Foreground" Value="#F59E0B"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </Border>
                        </Grid>
                    </Border>
                </StackPanel>
            </Border>

            <!-- Group 3: Браузер Edge, Поиск Bing и Ассистенты -->
            <StackPanel Orientation="Horizontal" Margin="0,4,0,8">
                <Border Background="#1A38BDF8" BorderBrush="#3338BDF8" BorderThickness="1" CornerRadius="5" Width="20" Height="20" Margin="0,0,6,0" VerticalAlignment="Center">
                    <Path Data="{StaticResource GeoNetwork}" Fill="{StaticResource IconGradSky}" Width="10" Height="10" Stretch="Uniform" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>
                <TextBlock Text="Браузер Microsoft Edge, поиск Bing и Copilot" FontSize="15" FontWeight="Bold" Foreground="{DynamicResource TextPrimaryBrush}" VerticalAlignment="Center"/>
            </StackPanel>

            <Border Background="{DynamicResource CardBackgroundBrush}" BorderBrush="{DynamicResource CardBorderBrush}" BorderThickness="1" CornerRadius="12" Padding="16" Margin="0,0,0,14">
                <StackPanel>
                    <!-- 7. Bing in Start Menu -->
                    <Border Background="{DynamicResource AppBackgroundBrush}" CornerRadius="8" Padding="12" Margin="0,0,0,8">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <CheckBox x:Name="Chk7" Grid.Column="0" IsChecked="{Binding IsBingSearchDisabled, Mode=TwoWay}" VerticalAlignment="Center" Margin="0,0,12,0"/>
                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                <TextBlock Text="Поиск Bing в меню «Пуск» и отправка поисковых запросов" FontSize="13" FontWeight="Bold" Foreground="{DynamicResource TextPrimaryBrush}"/>
                                <TextBlock Text="Отключить отправку веб-запросов из меню Пуск в поисковую систему Bing, оставив только локальный быстрый поиск по файлам." FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}" TextWrapping="Wrap" Margin="0,2,10,0"/>
                            </StackPanel>
                            <Border Grid.Column="2" CornerRadius="6" Padding="10,5" VerticalAlignment="Center" MinWidth="130">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Setter Property="Background" Value="#1A10B981"/>
                                        <Setter Property="BorderBrush" Value="#3310B981"/>
                                        <Setter Property="BorderThickness" Value="1"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding ElementName=Chk7, Path=IsChecked}" Value="False">
                                                <Setter Property="Background" Value="#1AF59E0B"/>
                                                <Setter Property="BorderBrush" Value="#33F59E0B"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <TextBlock HorizontalAlignment="Center" FontSize="10.5" FontWeight="Bold">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Text" Value="🛡️ ЗАЩИЩЕНО"/>
                                            <Setter Property="Foreground" Value="#10B981"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding ElementName=Chk7, Path=IsChecked}" Value="False">
                                                    <Setter Property="Text" Value="⚠️ СЛЕЖКА АКТИВНА"/>
                                                    <Setter Property="Foreground" Value="#F59E0B"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </Border>
                        </Grid>
                    </Border>

                    <!-- 8. Edge Telemetry -->
                    <Border Background="{DynamicResource AppBackgroundBrush}" CornerRadius="8" Padding="12" Margin="0,0,0,8">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <CheckBox x:Name="Chk8" Grid.Column="0" IsChecked="{Binding IsEdgeTelemetryDisabled, Mode=TwoWay}" VerticalAlignment="Center" Margin="0,0,12,0"/>
                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                <TextBlock Text="Телеметрия и фоновый трекинг Microsoft Edge" FontSize="13" FontWeight="Bold" Foreground="{DynamicResource TextPrimaryBrush}"/>
                                <TextBlock Text="Запретить браузеру Edge отправлять историю серфинга, поисковые подсказки и телеметрию диагностики." FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}" TextWrapping="Wrap" Margin="0,2,10,0"/>
                            </StackPanel>
                            <Border Grid.Column="2" CornerRadius="6" Padding="10,5" VerticalAlignment="Center" MinWidth="130">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Setter Property="Background" Value="#1A10B981"/>
                                        <Setter Property="BorderBrush" Value="#3310B981"/>
                                        <Setter Property="BorderThickness" Value="1"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding ElementName=Chk8, Path=IsChecked}" Value="False">
                                                <Setter Property="Background" Value="#1AF59E0B"/>
                                                <Setter Property="BorderBrush" Value="#33F59E0B"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <TextBlock HorizontalAlignment="Center" FontSize="10.5" FontWeight="Bold">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Text" Value="🛡️ ЗАЩИЩЕНО"/>
                                            <Setter Property="Foreground" Value="#10B981"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding ElementName=Chk8, Path=IsChecked}" Value="False">
                                                    <Setter Property="Text" Value="⚠️ СЛЕЖКА АКТИВНА"/>
                                                    <Setter Property="Foreground" Value="#F59E0B"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </Border>
                        </Grid>
                    </Border>

                    <!-- 9. Cortana & Windows Copilot -->
                    <Border Background="{DynamicResource AppBackgroundBrush}" CornerRadius="8" Padding="12">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <CheckBox x:Name="Chk9" Grid.Column="0" IsChecked="{Binding IsCortanaCopilotDisabled, Mode=TwoWay}" VerticalAlignment="Center" Margin="0,0,12,0"/>
                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                <TextBlock Text="Голосовой помощник Cortana и Windows Copilot" FontSize="13" FontWeight="Bold" Foreground="{DynamicResource TextPrimaryBrush}"/>
                                <TextBlock Text="Отключить фоновые службы ассистентов Cortana и Copilot, освободив оперативную память и фоновый процессорный ресурс." FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}" TextWrapping="Wrap" Margin="0,2,10,0"/>
                            </StackPanel>
                            <Border Grid.Column="2" CornerRadius="6" Padding="10,5" VerticalAlignment="Center" MinWidth="130">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Setter Property="Background" Value="#1A10B981"/>
                                        <Setter Property="BorderBrush" Value="#3310B981"/>
                                        <Setter Property="BorderThickness" Value="1"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding ElementName=Chk9, Path=IsChecked}" Value="False">
                                                <Setter Property="Background" Value="#1AF59E0B"/>
                                                <Setter Property="BorderBrush" Value="#33F59E0B"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <TextBlock HorizontalAlignment="Center" FontSize="10.5" FontWeight="Bold">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Text" Value="🛡️ ЗАЩИЩЕНО"/>
                                            <Setter Property="Foreground" Value="#10B981"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding ElementName=Chk9, Path=IsChecked}" Value="False">
                                                    <Setter Property="Text" Value="⚠️ СЛЕЖКА АКТИВНА"/>
                                                    <Setter Property="Foreground" Value="#F59E0B"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </Border>
                        </Grid>
                    </Border>
                </StackPanel>
            </Border>

            <!-- Group 4: Датчики, Сеть и Фоновый доступ -->
            <StackPanel Orientation="Horizontal" Margin="0,4,0,8">
                <Border Background="#1A34D399" BorderBrush="#3334D399" BorderThickness="1" CornerRadius="5" Width="20" Height="20" Margin="0,0,6,0" VerticalAlignment="Center">
                    <Path Data="{StaticResource GeoShield}" Fill="{StaticResource IconGradEmerald}" Width="10" Height="10" Stretch="Uniform" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>
                <TextBlock Text="Датчики геолокации, Wi-Fi Sense и фоновый доступ" FontSize="15" FontWeight="Bold" Foreground="{DynamicResource TextPrimaryBrush}" VerticalAlignment="Center"/>
            </StackPanel>

            <Border Background="{DynamicResource CardBackgroundBrush}" BorderBrush="{DynamicResource CardBorderBrush}" BorderThickness="1" CornerRadius="12" Padding="16">
                <StackPanel>
                    <!-- 10. Location Sensors -->
                    <Border Background="{DynamicResource AppBackgroundBrush}" CornerRadius="8" Padding="12" Margin="0,0,0,8">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <CheckBox x:Name="Chk10" Grid.Column="0" IsChecked="{Binding IsLocationSensorsDisabled, Mode=TwoWay}" VerticalAlignment="Center" Margin="0,0,12,0"/>
                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                <TextBlock Text="Датчики геолокации и служба определения местоположения" FontSize="13" FontWeight="Bold" Foreground="{DynamicResource TextPrimaryBrush}"/>
                                <TextBlock Text="Запретить операционной системе и приложениям постоянно определять точные географические координаты ПК." FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}" TextWrapping="Wrap" Margin="0,2,10,0"/>
                            </StackPanel>
                            <Border Grid.Column="2" CornerRadius="6" Padding="10,5" VerticalAlignment="Center" MinWidth="130">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Setter Property="Background" Value="#1A10B981"/>
                                        <Setter Property="BorderBrush" Value="#3310B981"/>
                                        <Setter Property="BorderThickness" Value="1"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding ElementName=Chk10, Path=IsChecked}" Value="False">
                                                <Setter Property="Background" Value="#1AF59E0B"/>
                                                <Setter Property="BorderBrush" Value="#33F59E0B"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <TextBlock HorizontalAlignment="Center" FontSize="10.5" FontWeight="Bold">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Text" Value="🛡️ ЗАЩИЩЕНО"/>
                                            <Setter Property="Foreground" Value="#10B981"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding ElementName=Chk10, Path=IsChecked}" Value="False">
                                                    <Setter Property="Text" Value="⚠️ СЛЕЖКА АКТИВНА"/>
                                                    <Setter Property="Foreground" Value="#F59E0B"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </Border>
                        </Grid>
                    </Border>

                    <!-- 11. Wi-Fi Sense -->
                    <Border Background="{DynamicResource AppBackgroundBrush}" CornerRadius="8" Padding="12" Margin="0,0,0,8">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <CheckBox x:Name="Chk11" Grid.Column="0" IsChecked="{Binding IsWifiSenseDisabled, Mode=TwoWay}" VerticalAlignment="Center" Margin="0,0,12,0"/>
                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                <TextBlock Text="Служба контроля сетей Wi-Fi Sense" FontSize="13" FontWeight="Bold" Foreground="{DynamicResource TextPrimaryBrush}"/>
                                <TextBlock Text="Отключить автоматическое подключение к открытым публичным хотспотам и передачу паролей сетей контактам." FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}" TextWrapping="Wrap" Margin="0,2,10,0"/>
                            </StackPanel>
                            <Border Grid.Column="2" CornerRadius="6" Padding="10,5" VerticalAlignment="Center" MinWidth="130">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Setter Property="Background" Value="#1A10B981"/>
                                        <Setter Property="BorderBrush" Value="#3310B981"/>
                                        <Setter Property="BorderThickness" Value="1"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding ElementName=Chk11, Path=IsChecked}" Value="False">
                                                <Setter Property="Background" Value="#1AF59E0B"/>
                                                <Setter Property="BorderBrush" Value="#33F59E0B"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <TextBlock HorizontalAlignment="Center" FontSize="10.5" FontWeight="Bold">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Text" Value="🛡️ ЗАЩИЩЕНО"/>
                                            <Setter Property="Foreground" Value="#10B981"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding ElementName=Chk11, Path=IsChecked}" Value="False">
                                                    <Setter Property="Text" Value="⚠️ СЛЕЖКА АКТИВНА"/>
                                                    <Setter Property="Foreground" Value="#F59E0B"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </Border>
                        </Grid>
                    </Border>

                    <!-- 12. Camera & Mic Background Access -->
                    <Border Background="{DynamicResource AppBackgroundBrush}" CornerRadius="8" Padding="12">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <CheckBox x:Name="Chk12" Grid.Column="0" IsChecked="{Binding IsCameraMicBackgroundDisabled, Mode=TwoWay}" VerticalAlignment="Center" Margin="0,0,12,0"/>
                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                <TextBlock Text="Блокировка скрытого фонового доступа к микрофону и камере" FontSize="13" FontWeight="Bold" Foreground="{DynamicResource TextPrimaryBrush}"/>
                                <TextBlock Text="Запретить UWP и фоновым процессам несанкционированно активировать микрофон и камеру без ведома пользователя." FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}" TextWrapping="Wrap" Margin="0,2,10,0"/>
                            </StackPanel>
                            <Border Grid.Column="2" CornerRadius="6" Padding="10,5" VerticalAlignment="Center" MinWidth="130">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Setter Property="Background" Value="#1A10B981"/>
                                        <Setter Property="BorderBrush" Value="#3310B981"/>
                                        <Setter Property="BorderThickness" Value="1"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding ElementName=Chk12, Path=IsChecked}" Value="False">
                                                <Setter Property="Background" Value="#1AF59E0B"/>
                                                <Setter Property="BorderBrush" Value="#33F59E0B"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <TextBlock HorizontalAlignment="Center" FontSize="10.5" FontWeight="Bold">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Text" Value="🛡️ ЗАЩИЩЕНО"/>
                                            <Setter Property="Foreground" Value="#10B981"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding ElementName=Chk12, Path=IsChecked}" Value="False">
                                                    <Setter Property="Text" Value="⚠️ СЛЕЖКА АКТИВНА"/>
                                                    <Setter Property="Foreground" Value="#F59E0B"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </Border>
                        </Grid>
                    </Border>
                </StackPanel>
            </Border>

            <!-- FAQ Section with Gradient Vector Icons -->
            <Border Background="{DynamicResource CardBackgroundBrush}" BorderBrush="{DynamicResource CardBorderBrush}" BorderThickness="1" CornerRadius="12" Padding="16" Margin="0,12,0,10">
                <StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                        <Border Background="#1A00D2FF" BorderBrush="#3300D2FF" BorderThickness="1" CornerRadius="4" Width="18" Height="18" Margin="0,0,8,0" VerticalAlignment="Center">
                            <Path Data="{StaticResource GeoQuestion}" Fill="{StaticResource IconGradCyan}" Width="10" Height="10" Stretch="Uniform" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <TextBlock Text="Частые вопросы по Защите приватности (FAQ)" FontSize="13.5" FontWeight="Bold" Foreground="{DynamicResource TextPrimaryBrush}" VerticalAlignment="Center"/>
                    </StackPanel>

                    <Expander Foreground="{DynamicResource TextPrimaryBrush}" FontSize="12" FontWeight="SemiBold">
                        <Expander.Header>
                            <StackPanel Orientation="Horizontal">
                                <Path Data="{StaticResource GeoShield}" Fill="{StaticResource IconGradEmerald}" Width="12" Height="12" Stretch="Uniform" Margin="0,0,8,0" VerticalAlignment="Center"/>
                                <TextBlock Text="Не сломает ли отключение телеметрии работу Windows и магазина приложений?" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Expander.Header>
                        <TextBlock Text="Нет. Все твики приватности STORM блокируют исключительно фоновый трекинг, запись нажатий клавиш (кейлоггинг) и отправку диагностических снимков экрана в Microsoft. Windows Update, магазин Microsoft Store и учетные записи продолжают работать в штатном режиме." FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}" Margin="10,2,0,2" TextWrapping="Wrap"/>
                    </Expander>
                </StackPanel>
            </Border>

        </StackPanel>
    </ScrollViewer>
</Page>
