<Page
    x:Class="StormSystemOptimizer.Views.StartupPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:StormSystemOptimizer.Views"
    xmlns:models="using:StormSystemOptimizer.Models"
    xmlns:vm="using:StormSystemOptimizer.ViewModels"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="{ThemeResource StormCardGradient}" BorderBrush="{ThemeResource CardBorderBrush}" BorderThickness="1" CornerRadius="12" Padding="20" Margin="0,0,0,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Spacing="4">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <FontIcon Glyph="&#xE7B5;" FontSize="20" Foreground="{ThemeResource AccentPrimaryBrush}"/>
                        <TextBlock Text="Менеджер автозагрузки Windows" FontSize="20" FontWeight="Bold" Foreground="{ThemeResource TextPrimaryBrush}"/>
                    </StackPanel>
                    <TextBlock Text="{x:Bind ViewModel.StatusText, Mode=OneWay}" FontSize="13" Foreground="{ThemeResource TextSecondaryBrush}"/>
                </StackPanel>

                <Button Grid.Column="1" Click="BtnRefresh_Click"
                        Background="{ThemeResource CardBackgroundBrush}" BorderBrush="{ThemeResource CardBorderBrush}"
                        Foreground="{ThemeResource TextPrimaryBrush}" CornerRadius="8" Padding="14,8">
                    <StackPanel Orientation="Horizontal" Spacing="6">
                        <FontIcon Glyph="&#xE72C;" FontSize="14" Foreground="{ThemeResource AccentPrimaryBrush}"/>
                        <TextBlock Text="Обновить список"/>
                    </StackPanel>
                </Button>
            </Grid>
        </Border>

        <!-- Startup Items List -->
        <ListView Grid.Row="1" ItemsSource="{x:Bind ViewModel.StartupItems, Mode=OneWay}" SelectionMode="None">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="models:StartupEntry">
                    <Border Background="{ThemeResource CardBackgroundBrush}" BorderBrush="{ThemeResource CardBorderBrush}" BorderThickness="1" CornerRadius="10" Padding="16,12" Margin="0,0,0,8">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0" Spacing="3" VerticalAlignment="Center">
                                <StackPanel Orientation="Horizontal" Spacing="8">
                                    <TextBlock Text="{x:Bind Name}" FontSize="14" FontWeight="Bold" Foreground="{ThemeResource TextPrimaryBrush}"/>
                                    <Border Background="{ThemeResource CardHoverBrush}" CornerRadius="4" Padding="6,2">
                                        <TextBlock Text="{x:Bind Location}" FontSize="9" Foreground="{ThemeResource TextSecondaryBrush}"/>
                                    </Border>
                                </StackPanel>
                                <TextBlock Text="{x:Bind Publisher}" FontSize="12" Foreground="{ThemeResource TextSecondaryBrush}"/>
                                <TextBlock Text="{x:Bind Command}" FontSize="11" Foreground="{ThemeResource TextMutedBrush}" TextTrimming="CharacterEllipsis"/>
                            </StackPanel>

                            <!-- Impact Badge -->
                            <Border Grid.Column="1" Background="{ThemeResource CardHoverBrush}" CornerRadius="6" Padding="8,4" VerticalAlignment="Center" Margin="12,0,16,0">
                                <StackPanel Orientation="Horizontal" Spacing="4">
                                    <TextBlock Text="Влияние:" FontSize="11" Foreground="{ThemeResource TextSecondaryBrush}"/>
                                    <TextBlock Text="{x:Bind Impact}" FontSize="11" FontWeight="Bold" Foreground="{ThemeResource AccentPrimaryBrush}"/>
                                </StackPanel>
                            </Border>

                            <!-- Toggle Switch -->
                            <ToggleSwitch Grid.Column="2" IsOn="{x:Bind IsEnabled, Mode=TwoWay}"
                                          Toggled="ToggleSwitch_Toggled"
                                          OnContent="Включено" OffContent="Отключено" VerticalAlignment="Center"/>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
