<UserControl x:Class="StormSystemOptimizer.Controls.CircularGaugeControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="140" d:DesignWidth="140">

    <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
        <!-- Background Track Arc -->
        <Path x:Name="TrackPath"
              Stroke="{Binding Path=TrackBrush, RelativeSource={RelativeSource AncestorType=UserControl}}"
              StrokeThickness="{Binding Path=StrokeThickness, RelativeSource={RelativeSource AncestorType=UserControl}}"
              StrokeStartLineCap="Round"
              StrokeEndLineCap="Round"/>

        <!-- Active Gradient Progress Arc -->
        <Path x:Name="ProgressPath"
              Stroke="{Binding Path=ArcBrush, RelativeSource={RelativeSource AncestorType=UserControl}}"
              StrokeThickness="{Binding Path=StrokeThickness, RelativeSource={RelativeSource AncestorType=UserControl}}"
              StrokeStartLineCap="Round"
              StrokeEndLineCap="Round"
              Effect="{DynamicResource StormGlowEffect}"/>

        <!-- Glowing Endpoint Marker Dot -->
        <Canvas HorizontalAlignment="Left" VerticalAlignment="Top" Width="140" Height="140" IsHitTestVisible="False">
            <Ellipse x:Name="EndpointMarker"
                     Width="10" Height="10"
                     Fill="#FFFFFF"
                     Stroke="{Binding Path=ArcBrush, RelativeSource={RelativeSource AncestorType=UserControl}}"
                     StrokeThickness="2"
                     Effect="{DynamicResource StormGlowEffect}"/>
        </Canvas>

        <!-- Center Value & Subtitle -->
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,4,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <TextBlock x:Name="TxtValue"
                           Text="0"
                           FontSize="22"
                           FontWeight="Bold"
                           Foreground="{DynamicResource TextPrimaryBrush}"
                           HorizontalAlignment="Center"/>
                <TextBlock x:Name="TxtUnit"
                           Text="%"
                           FontSize="12"
                           FontWeight="SemiBold"
                           Foreground="{DynamicResource TextSecondaryBrush}"
                           VerticalAlignment="Bottom"
                           Margin="2,0,0,3"/>
            </StackPanel>
            <TextBlock x:Name="TxtTitle"
                       Text="LOAD"
                       FontSize="9.5"
                       FontWeight="Bold"
                       Foreground="{DynamicResource TextSecondaryBrush}"
                       HorizontalAlignment="Center"
                       Margin="0,2,0,0"/>
        </StackPanel>
    </Grid>
</UserControl>
