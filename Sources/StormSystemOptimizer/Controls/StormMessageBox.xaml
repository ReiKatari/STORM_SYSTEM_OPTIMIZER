<Window x:Class="StormSystemOptimizer.Controls.StormMessageBox"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        Title="STORM Message"
        Height="220" Width="480"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">

    <Border Background="{DynamicResource CardBackgroundBrush}"
            BorderBrush="{DynamicResource AccentPrimaryBrush}"
            BorderThickness="1.5"
            CornerRadius="12">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="38"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="56"/>
            </Grid.RowDefinitions>

            <!-- Custom Dark Title Bar -->
            <Border Grid.Row="0" Background="{DynamicResource NavSidebarBrush}" CornerRadius="11,11,0,0" Padding="14,0" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid>
                    <TextBlock x:Name="TxtTitle" Text="STORM SYSTEM OPTIMIZER" FontSize="12" FontWeight="Bold" Foreground="{DynamicResource AccentPrimaryBrush}" VerticalAlignment="Center"/>
                    <Button Click="BtnClose_Click" HorizontalAlignment="Right" Width="28" Height="28" Background="Transparent" BorderThickness="0" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="11" Cursor="Hand" Content="✕">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="bd" Background="{TemplateBinding Background}" CornerRadius="4">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="bd" Property="Background" Value="#E81123"/>
                                        <Setter Property="Foreground" Value="#FFFFFF"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </Grid>
            </Border>

            <!-- Content Area (Icon + Message) -->
            <Grid Grid.Row="1" Margin="20,14,20,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Icon Badge -->
                <Border x:Name="IconBadge" Grid.Column="0" Width="42" Height="42" CornerRadius="21"
                        Background="{DynamicResource AccentGlowBrush}"
                        BorderBrush="{DynamicResource AccentPrimaryBrush}"
                        BorderThickness="1"
                        VerticalAlignment="Top" Margin="0,2,16,0">
                    <TextBlock x:Name="TxtIcon" Text="ℹ️" FontSize="18" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>

                <!-- Message Text -->
                <ScrollViewer Grid.Column="1" VerticalScrollBarVisibility="Auto">
                    <TextBlock x:Name="TxtMessage" Text="Сообщение" FontSize="13" Foreground="{DynamicResource TextPrimaryBrush}"
                               TextWrapping="Wrap" LineHeight="18" VerticalAlignment="Center"/>
                </ScrollViewer>
            </Grid>

            <!-- Button Footer -->
            <Border Grid.Row="2" Background="{DynamicResource NavSidebarBrush}" CornerRadius="0,0,11,11" Padding="16,10">
                <StackPanel x:Name="PnlButtons" Orientation="Horizontal" HorizontalAlignment="Right">
                    <!-- Dynamic Buttons created in Code-Behind (OK, Cancel, Yes, No) -->
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</Window>
